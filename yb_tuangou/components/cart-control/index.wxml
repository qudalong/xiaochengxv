<view class="operation">
  <view wx:if="{{goodCount>0}}" class="iconfont icon-jian"></view>
  <view wx:if="{{goodCount>0}}" class='count'>{{goodCount}}</view>
  <view class='iconfont icon-jia'></view>
</view>